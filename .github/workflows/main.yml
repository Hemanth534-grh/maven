name: My First GitHub Action

on:
  pull_request: 

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello 👋 GitHub Actions is working Good!,Thank you."
